
<div class="container ">
    <div class="row justify-content-center align-items-center text-center">
        <div class="card" id="more-info-card" style="width: 55rem;">

            <img class="card-img-top" id="more-info-img" src="{{gem.image}}" alt="Card image cap">
            <div class="card-body">
                <h5 class="card-title" id="more-info-title">{{gem.title}}</h5>
                <p class="card-text" id="more-info-description">{{gem.description}}</p>
                <p class="card-text">
                    <spam>Address:</spam> {{gem.address}}</p>
                <p class="card-text sub">
                    <spam>Added by:</spam> {{gem.User.username}}</p>
            </div>
            {{#if user.owner}}
            <form id="post_delete">
                <button class="btn btn-xs btn-danger deleteButton" type="submit" data-id="{{gem.id}}">DELETE</button>
            </form>
            {{/if}} {{#if user}}
            
            <form action="/gems/comments/{{gem.id}}/add" method="POST">
                <div class="form-group">
                    <p>Add Comment:
                        <p>
                            <input class="form-control" type="text" name="comment" palseholder="text">
                </div>
                <button id="login-btn" type="submit" class="btn btn-outline-primary">Submit!</button>
            </form>

                <form action="/gems/comments/{{gem.id}}/all" method="GET">
                    <button class="btn btn-outline-primary " type="submit" data-id="{{gem.id}}">See Comments</button>
                </form>


            <form class="put_update">
                <button class="btn btn-link like-btn" type="submit" data-id="{{gem.id}}">LIKE</button> {{gem.likes}} --{{gem.id}}
            </form>

                

            {{/if}}
            

        </div>
   </div>
</div>