<div class="container">
     {{#if comments}} 
     {{#each comments}}
<div class="card" style="width: 45rem;">
    <div class="card-header">
        {{this.User.username}}
        
    </div>
    <ul class="list-group list-group-flush">
        <li class="list-group-item">{{this.Comment}}</li>
        <li class="list-group-item">{{this.createdAt}}</li>
        
        
    </ul>
</div>
<div class="form-group">
    {{!--<a href="/gems/{{this.UserPostId}}" class="btn btn-primary btn-outline-primary">Back</a>--}}
    <p></p>
    {{else}}
    <p>No Comments</p>
    {{/each }} {{/if}}
    <p></p>
    {{#if curPostId }}
     <p> USER POST ID  {{curPostId}}</p>
    <a href="/gems/{{curPostId}}" class="btn btn-primary btn-outline-primary">Back</a>
    {{/if}}
    
<a href="/gems/all" class="btn btn-primary btn-outline-primary">Back</a>
</div>
</div>