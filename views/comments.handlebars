<div class="container"
<div class="form-group">
    {{#if comments}} {{#each comments}}
    <p>{{Comment}}</p>
    {{/each }} {{/if}}
    <p></p>
    <button id="login-btn" type="submit" class="btn btn-outline-primary">Back</button>
</div>
</div>